<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Phone</th>
        </tr>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <tr>
                <td><%= user.name %></td>
                <td><%= user.phone %></td>
            </tr>
        <% end %>
    </tbody>
</table>

<%= link_to 'Click Here!', users_new_path, class: 'btn-click-here' %>